# Base runtime environment for this repository.
# Versions validated for the Qwen3-MoE pruning, PPL, and EvalScope workflows.

torch==2.8.0
triton==3.4.0
transformers==5.6.2
accelerate==1.12.0
datasets==3.6.0
evalscope==1.4.1

# Optional extras for non-default paths:
# flash-attn==2.8.3  # install separately with: pip install flash-attn==2.8.3 --no-build-isolation
